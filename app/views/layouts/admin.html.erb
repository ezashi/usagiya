<!DOCTYPE html>
<html>

<head>
    <title>うさぎや 管理画面 - <%= content_for(:title) || "Dashboard" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
</head>

<body class="bg-gray-100">
    <% if admin_logged_in? %>
    <nav class="bg-red-600 text-white shadow-lg">
        <div class="container mx-auto px-4">
            <div class="flex justify-between items-center py-4">
                <div class="flex items-center space-x-4">
                    <%= link_to "うさぎや 管理画面", admin_root_path, class: "text-xl font-bold" %>
                </div>

                <div class="flex items-center space-x-6">
                    <%= link_to "ダッシュボード", admin_root_path, class: "hover:text-gray-200" %>
                    <%= link_to "商品管理", admin_products_path, class: "hover:text-gray-200" %>
                    <%= link_to "お知らせ", admin_notices_path, class: "hover:text-gray-200" %>
                    <%= link_to "営業カレンダー", admin_calendar_events_path, class: "hover:text-gray-200" %>
                    <%= link_to "注文一覧", admin_orders_path, class: "hover:text-gray-200" %>
                    <%= link_to "お問い合わせ", admin_inquiries_path, class: "hover:text-gray-200" %>
                    <%= button_to "ログアウト", admin_logout_path, method: :delete, class: "hover:text-gray-200" %>
                </div>
            </div>
        </div>
    </nav>
    <% end %>

    <main class="<%= admin_logged_in? ? 'container mx-auto px-4 py-8' : '' %>">
        <% if flash[:notice] %>
        <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded mb-4">
            <%= flash[:notice] %>
        </div>
        <% end %>

        <% if flash[:alert] %>
        <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded mb-4">
            <%= flash[:alert] %>
        </div>
        <% end %>

        <%= yield %>
    </main>
</body>

</html>