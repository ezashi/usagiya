<% content_for :title, "ご注文内容確認 - うさぎや" %>
<% content_for :description, "ご注文内容の確認ページ" %>

<div class="w-full max-w-4xl mx-auto">
    <!-- ページヘッダー -->
    <div class="mb-12 text-center">
        <h1 class="text-4xl md:text-5xl font-bold text-gray-800 mb-4">ご注文内容確認</h1>
        <p class="text-gray-600 text-lg">内容をご確認の上、ご注文を確定してください</p>
    </div>

    <!-- ステップインジケーター -->
    <div class="bg-white rounded-2xl shadow-lg p-6 mb-8">
        <div class="flex items-center justify-between">
            <div class="flex-1 flex items-center">
                <div class="w-10 h-10 bg-green-500 text-white rounded-full flex items-center justify-center font-bold">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                    </svg>
                </div>
                <div class="ml-3">
                    <p class="font-bold text-gray-800">入力</p>
                    <p class="text-xs text-gray-500">情報を入力</p>
                </div>
            </div>
            <svg class="w-6 h-6 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
            </svg>
            <div class="flex-1 flex items-center justify-center">
                <div class="w-10 h-10 bg-pink-600 text-white rounded-full flex items-center justify-center font-bold">2</div>
                <div class="ml-3">
                    <p class="font-bold text-gray-800">確認</p>
                    <p class="text-xs text-gray-500">内容を確認</p>
                </div>
            </div>
            <svg class="w-6 h-6 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
            </svg>
            <div class="flex-1 flex items-center justify-end">
                <div class="w-10 h-10 bg-gray-200 text-gray-500 rounded-full flex items-center justify-center font-bold">3</div>
                <div class="ml-3">
                    <p class="font-bold text-gray-500">完了</p>
                    <p class="text-xs text-gray-400">注文完了</p>
                </div>
            </div>
        </div>
    </div>

    <%= form_with model: @order, url: orders_path, method: :post do |f| %>
    <!-- Hidden fields to preserve form data -->
    <%= f.hidden_field :customer_name %>
    <%= f.hidden_field :postal_code %>
    <%= f.hidden_field :address %>
    <%= f.hidden_field :phone %>
    <%= f.hidden_field :email %>
    <%= f.hidden_field :same_address %>
    <%= f.hidden_field :delivery_name unless @order.same_address? %>
    <%= f.hidden_field :delivery_postal_code unless @order.same_address? %>
    <%= f.hidden_field :delivery_address unless @order.same_address? %>
    <%= f.hidden_field :delivery_phone unless @order.same_address? %>
    <%= f.hidden_field :payment_method %>
    <%= f.hidden_field :delivery_date %>
    <%= f.hidden_field :delivery_time %>
    <%= f.hidden_field :wrapping_type %>
    <%= f.hidden_field :notes %>

    <%= f.fields_for :order_items do |item_form| %>
    <%= item_form.hidden_field :product_type %>
    <%= item_form.hidden_field :quantity %>
    <% end %>

    <!-- ご注文者様情報 -->
    <div class="bg-white rounded-2xl shadow-lg p-6 md:p-8 mb-6">
        <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center pb-4 border-b-2 border-gray-100">
            <svg class="w-7 h-7 mr-2 text-pink-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
            </svg>
            ご注文者様情報
        </h2>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div class="bg-gray-50 rounded-xl p-4">
                <p class="text-sm text-gray-600 mb-1 font-medium">お名前</p>
                <p class="text-lg font-bold text-gray-800"><%= @order.customer_name %></p>
            </div>

            <div class="bg-gray-50 rounded-xl p-4">
                <p class="text-sm text-gray-600 mb-1 font-medium">電話番号</p>
                <p class="text-lg font-bold text-gray-800"><%= @order.phone %></p>
            </div>

            <div class="bg-gray-50 rounded-xl p-4">
                <p class="text-sm text-gray-600 mb-1 font-medium">郵便番号</p>
                <p class="text-lg font-bold text-gray-800"><%= @order.postal_code %></p>
            </div>

            <div class="bg-gray-50 rounded-xl p-4">
                <p class="text-sm text-gray-600 mb-1 font-medium">メールアドレス</p>
                <p class="text-lg font-bold text-gray-800 break-all"><%= @order.email %></p>
            </div>

            <div class="bg-gray-50 rounded-xl p-4 md:col-span-2">
                <p class="text-sm text-gray-600 mb-1 font-medium">住所</p>
                <p class="text-lg font-bold text-gray-800"><%= @order.address %></p>
            </div>
        </div>
    </div>

    <!-- お送り先情報 -->
    <div class="bg-white rounded-2xl shadow-lg p-6 md:p-8 mb-6">
        <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center pb-4 border-b-2 border-gray-100">
            <svg class="w-7 h-7 mr-2 text-pink-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path d="M9 17a2 2 0 11-4 0 2 2 0 014 0zM19 17a2 2 0 11-4 0 2 2 0 014 0z"></path>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16V6a1 1 0 00-1-1H4a1 1 0 00-1 1v10a1 1 0 001 1h1m8-1a1 1 0 01-1 1H9m4-1V8a1 1 0 011-1h2.586a1 1 0 01.707.293l3.414 3.414a1 1 0 01.293.707V16a1 1 0 01-1 1h-1m-6-1a1 1 0 001 1h1M5 17a2 2 0 104 0m-4 0a2 2 0 114 0m6 0a2 2 0 104 0m-4 0a2 2 0 114 0"></path>
            </svg>
            お送り先
        </h2>

        <% if @order.same_address? %>
        <div class="bg-pink-50 border-2 border-pink-200 rounded-xl p-6 text-center">
            <svg class="w-12 h-12 mx-auto text-pink-600 mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <p class="text-lg font-bold text-gray-800">ご注文者様と同じ住所</p>
        </div>
        <% else %>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div class="bg-gray-50 rounded-xl p-4">
                <p class="text-sm text-gray-600 mb-1 font-medium">お名前</p>
                <p class="text-lg font-bold text-gray-800"><%= @order.delivery_name %></p>
            </div>

            <div class="bg-gray-50 rounded-xl p-4">
                <p class="text-sm text-gray-600 mb-1 font-medium">電話番号</p>
                <p class="text-lg font-bold text-gray-800"><%= @order.delivery_phone %></p>
            </div>

            <div class="bg-gray-50 rounded-xl p-4">
                <p class="text-sm text-gray-600 mb-1 font-medium">郵便番号</p>
                <p class="text-lg font-bold text-gray-800"><%= @order.delivery_postal_code %></p>
            </div>

            <div class="bg-gray-50 rounded-xl p-4 md:col-span-2">
                <p class="text-sm text-gray-600 mb-1 font-medium">住所</p>
                <p class="text-lg font-bold text-gray-800"><%= @order.delivery_address %></p>
            </div>
        </div>
        <% end %>
    </div>

    <!-- ご注文内容 -->
    <div class="bg-white rounded-2xl shadow-lg p-6 md:p-8 mb-6">
        <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center pb-4 border-b-2 border-gray-100">
            <svg class="w-7 h-7 mr-2 text-pink-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"></path>
            </svg>
            ご注文内容
        </h2>

        <div class="overflow-x-auto">
            <table class="w-full">
                <thead class="bg-gray-100">
                    <tr>
                        <th class="px-6 py-4 text-left text-sm font-bold text-gray-700">商品名</th>
                        <th class="px-6 py-4 text-right text-sm font-bold text-gray-700">数量</th>
                        <th class="px-6 py-4 text-right text-sm font-bold text-gray-700">単価</th>
                        <th class="px-6 py-4 text-right text-sm font-bold text-gray-700">小計</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-200">
                    <% @order.order_items.reject { |item| item.quantity.to_i == 0 }.each do |item| %>
                    <tr class="hover:bg-gray-50 transition-colors">
                        <td class="px-6 py-4 font-medium text-gray-800"><%= item.product_name %></td>
                        <td class="px-6 py-4 text-right text-gray-700"><%= item.quantity %>個</td>
                        <td class="px-6 py-4 text-right text-gray-700">¥<%= number_with_delimiter(item.price_per_unit) %></td>
                        <td class="px-6 py-4 text-right font-bold text-gray-800">¥<%= number_with_delimiter(item.subtotal) %></td>
                    </tr>
                    <% end %>
                </tbody>
                <tfoot class="bg-pink-50 border-t-2 border-pink-200">
                    <tr>
                        <td colspan="3" class="px-6 py-4 text-right text-lg font-bold text-gray-800">合計金額</td>
                        <td class="px-6 py-4 text-right text-2xl font-bold text-pink-600">
                            ¥<%= number_with_delimiter(@order.order_items.sum(&:subtotal)) %>
                        </td>
                    </tr>
                </tfoot>
            </table>
        </div>
        <p class="text-sm text-gray-500 text-right mt-4">※送料は別途かかります</p>
    </div>

    <!-- 配送・お支払い情報 -->
    <div class="bg-white rounded-2xl shadow-lg p-6 md:p-8 mb-6">
        <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center pb-4 border-b-2 border-gray-100">
            <svg class="w-7 h-7 mr-2 text-pink-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z"></path>
            </svg>
            配送・お支払い情報
        </h2>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <% if @order.delivery_date.present? %>
            <div class="bg-gray-50 rounded-xl p-4">
                <p class="text-sm text-gray-600 mb-1 font-medium">到着希望日</p>
                <p class="text-lg font-bold text-gray-800">
                    <%= @order.delivery_date.strftime("%Y年%m月%d日") %>
                    (<%= %w[日 月 火 水 木 金 土][@order.delivery_date.wday] %>)
                </p>
            </div>
            <% end %>

            <% if @order.delivery_time.present? %>
            <div class="bg-gray-50 rounded-xl p-4">
                <p class="text-sm text-gray-600 mb-1 font-medium">希望到着時間</p>
                <p class="text-lg font-bold text-gray-800"><%= @order.delivery_time %></p>
            </div>
            <% end %>

            <div class="bg-gray-50 rounded-xl p-4">
                <p class="text-sm text-gray-600 mb-1 font-medium">お支払い方法</p>
                <p class="text-lg font-bold text-gray-800">
                    <%= Order.payment_method_options.key(@order.payment_method) %>
                </p>
            </div>

            <% if @order.wrapping_type.present? %>
            <div class="bg-gray-50 rounded-xl p-4">
                <p class="text-sm text-gray-600 mb-1 font-medium">包装</p>
                <p class="text-lg font-bold text-gray-800"><%= @order.wrapping_type %></p>
            </div>
            <% end %>

            <% if @order.notes.present? %>
            <div class="bg-gray-50 rounded-xl p-4 md:col-span-2">
                <p class="text-sm text-gray-600 mb-2 font-medium">ご希望欄</p>
                <p class="text-gray-800 whitespace-pre-wrap leading-relaxed"><%= @order.notes %></p>
            </div>
            <% end %>
        </div>
    </div>

    <!-- 確認事項 -->
    <div class="bg-yellow-50 border-2 border-yellow-200 rounded-2xl p-6 mb-8">
        <h3 class="font-bold text-gray-800 mb-4 flex items-center text-lg">
            <svg class="w-6 h-6 mr-2 text-yellow-600" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path>
            </svg>
            ご確認ください
        </h3>
        <ul class="space-y-2 text-sm text-gray-700">
            <li class="flex items-start">
                <svg class="w-5 h-5 mr-2 text-yellow-600 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                </svg>
                <span>上記の内容でご注文を確定します</span>
            </li>
            <li class="flex items-start">
                <svg class="w-5 h-5 mr-2 text-yellow-600 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                </svg>
                <span>ご注文完了後、確認メールをお送りします</span>
            </li>
            <li class="flex items-start">
                <svg class="w-5 h-5 mr-2 text-yellow-600 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                </svg>
                <span>内容に誤りがある場合は「内容を修正する」ボタンから修正してください</span>
            </li>
        </ul>
    </div>

    <!-- アクションボタン -->
    <div class="flex flex-col sm:flex-row gap-4">
        <button type="button" onclick="history.back()" class="flex-1 bg-gray-200 hover:bg-gray-300 text-gray-700 font-bold py-4 px-6 rounded-xl transition-colors flex items-center justify-center">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 17l-5-5m0 0l5-5m-5 5h12"></path>
            </svg>
            内容を修正する
        </button>

        <%= f.submit "この内容で注文を確定する", 
        class: "flex-1 bg-pink-600 hover:bg-pink-700 text-white font-bold py-4 px-6 rounded-xl transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl cursor-pointer flex items-center justify-center",
        data: { confirm: "この内容でご注文を確定してよろしいですか？", disable_with: "送信中..." } %>
    </div>
    <% end %>
</div>