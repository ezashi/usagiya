<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>御菓子処うさぎや - トップページ</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Noto Sans JP', sans-serif;
            line-height: 1.6;
            color: #EE827C;
            overflow-x: hidden;
        }

        /* ヘッダー - 画像の上に重ねる */
        header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 1000;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 30px 40px;
            background: linear-gradient(to bottom, rgba(0, 0, 0, 0.5) 0%, rgba(0, 0, 0, 0) 100%);
        }

        .logo {
            display: flex;
            align-items: center;
        }

        .logo a {
            display: block;
            line-height: 0;
        }

        .logo-image {
            height: 100px;
            width: auto;
            object-fit: contain;
            filter: drop-shadow(2px 2px 4px rgba(0, 0, 0, 0.5));
            transition: opacity 0.3s;
        }

        .logo-image:hover {
            opacity: 0.8;
        }

        /* ナビゲーション - 常に表示 */
        nav {
            display: flex;
            gap: 30px;
            align-items: center;
        }

        nav a {
            text-decoration: none;
            color: white;
            font-weight: 500;
            transition: all 0.3s;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            white-space: nowrap;
        }

        nav a:hover {
            color: #EE827C;
            transform: translateY(-2px);
        }

        /* ハンバーガーメニュー - 横棒が近づく動き */
        .hamburger {
            display: flex;
            flex-direction: column;
            cursor: pointer;
            gap: 8px;
            padding: 10px;
            margin-left: 20px;
        }

        .hamburger span {
            width: 30px;
            height: 3px;
            background: white;
            transition: all 0.3s ease;
            border-radius: 2px;
            box-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        /* ホバー時：上の横棒は下に、下の横棒は上に */
        .hamburger:hover span:first-child {
            transform: translateY(4px);
            background: #EE827C;
        }

        .hamburger:hover span:last-child {
            background: #EE827C;
        }

        /* クリック時 */
        .hamburger:active span {
            background: #EE827C;
        }

        /* オーバーレイとサイドメニュー */
        .overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            z-index: 1999;
        }

        .overlay.active {
            display: block;
        }

        .side-menu {
            position: fixed;
            top: 0;
            right: -300px;
            width: 300px;
            height: 100%;
            background: white;
            box-shadow: -2px 0 5px rgba(0, 0, 0, 0.3);
            transition: right 0.3s;
            z-index: 2000;
            overflow-y: auto;
        }

        .side-menu.active {
            right: 0;
        }

        .side-menu-close {
            text-align: right;
            font-size: 2rem;
            padding: 20px;
            cursor: pointer;
            color: #d4006a;
            transition: color 0.3s;
        }

        .side-menu-close:hover {
            color: #EE827C;
        }

        .side-menu-links {
            list-style: none;
            padding: 20px;
        }

        .side-menu-links li {
            margin: 20px 0;
        }

        .side-menu-links a {
            text-decoration: none;
            color: #333;
            font-size: 1.1rem;
            display: block;
            padding: 10px;
            transition: all 0.3s;
        }

        .side-menu-links a:hover {
            background: #f0f0f0;
            color: #EE827C;
            transform: translateX(5px);
        }

        /* メインビジュアル - 画面いっぱい（レスポンシブ対応） */
        .main-visual {
            position: relative;
            width: 100%;
            height: 100vh;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            background: #000;
        }

        .slide {
            position: absolute;
            width: 100%;
            height: 100%;
            opacity: 0;
            transition: opacity 1s;
            display: flex;
            align-items: center;
            justify-content: center;
            pointer-events: none;
            /* デフォルトでクリック無効 */
        }

        .slide.active {
            opacity: 1;
            pointer-events: auto;
            /* activeの時のみクリック有効 */
        }

        /* クリック可能なスライド以外はクリック無効のまま */
        .slide.active:not(.slide-clickable) {
            pointer-events: none;
        }

        /* クリック可能なスライドのみクリック有効 */
        .slide.active.slide-clickable {
            pointer-events: auto;
        }

        .slide img {
            max-width: 100%;
            max-height: 100%;
            width: 100%;
            height: 100%;
            object-fit: cover;
            object-position: center;
        }

        /* コンテンツエリア */
        .content-wrapper {
            position: relative;
            z-index: 1;
        }

        /* 店舗情報セクション */
        .shop-info {
            padding: 80px 40px;
            background: #f9f9f9;
        }

        .shop-info h2 {
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 60px;
            color: #D5016B;
            text-align: center;
        }

        .info-grid-new {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 60px;
            max-width: 1200px;
            margin: 0 auto;
            align-items: start;
        }

        .info-column {
            display: flex;
            flex-direction: column;
            gap: 30px;
            align-items: center;
        }

        .info-column:nth-child(2) {
            justify-content: center;
        }

        .info-section {
            text-align: center;
        }

        .info-section h3 {
            font-size: 1.4rem;
            font-weight: bold;
            margin-bottom: 15px;
            color: #D5016B;
        }

        .info-section p {
            line-height: 1.8;
            margin: 5px 0;
        }

        .postal-code {
            font-size: 1.1rem;
            font-weight: 500;
        }

        .address {
            font-size: 1.1rem;
        }

        .map-link {
            color: #F0908D;
            text-decoration: none;
            font-weight: 500;
        }

        .map-link:hover {
            text-decoration: underline;
        }

        .business-hours {
            font-size: 1.1rem;
        }

        .closed-day {
            font-size: 1.1rem;
        }

        .irregular-holiday {
            font-size: 1.1rem;
        }

        .note {
            font-size: 0.9rem;
            margin-top: 10px;
            text-align: left !important;
            max-width: 280px;
            margin-left: auto;
            margin-right: auto;
            line-height: 1.6;
        }

        .phone {
            font-size: 1.1rem;
        }

        .phone a {
            color: #F0908D;
            text-decoration: none;
        }

        .phone a:hover {
            text-decoration: underline;
        }

        .sns-links {
            display: flex;
            gap: 15px;
            margin-top: 10px;
            justify-content: center;
        }

        .sns-icon-img {
            display: block;
            transition: opacity 0.3s;
        }

        .sns-icon-img:hover {
            opacity: 0.7;
        }

        .sns-image {
            width: 50px;
            height: 50px;
            object-fit: contain;
        }

        /* レスポンシブ */
        @media (max-width: 1024px) {
            header {
                padding: 20px 30px;
            }

            nav {
                gap: 20px;
            }

            nav a {
                font-size: 0.9rem;
            }

            .main-visual {
                height: 80vh;
            }

            .slide img {
                object-fit: contain;
            }

            .info-grid-new {
                grid-template-columns: 1fr;
                gap: 40px;
            }

            .shop-info h2 {
                font-size: 2rem;
            }

            .note {
                text-align: left !important;
                max-width: 280px;
                margin-left: auto;
                margin-right: auto;
            }

            .logo-image {
                height: 80px;
            }
        }

        @media (max-width: 768px) {
            header {
                padding: 20px;
            }

            .logo-image {
                height: 55px;
            }

            nav {
                gap: 15px;
            }

            nav a {
                font-size: 0.85rem;
            }

            .hamburger {
                margin-left: 10px;
            }

            .hamburger span {
                width: 25px;
            }

            .main-visual {
                height: 60vh;
                min-height: 400px;
            }

            .slide img {
                object-fit: contain;
            }

            .shop-info {
                padding: 60px 20px;
            }

            .shop-info h2 {
                font-size: 1.75rem;
                margin-bottom: 40px;
            }

            .info-section h3 {
                font-size: 1.2rem;
            }

            .note {
                text-align: left !important;
                max-width: 320px;
                margin: 10px auto 0 auto;
                font-size: 0.85rem;
            }
        }

        @media (max-width: 480px) {
            header {
                padding: 10px;
            }

            .logo-image {
                height: 0px;
            }

            nav {
                gap: 10px;
            }

            nav a {
                font-size: 0.75rem;
            }

            .hamburger span {
                width: 22px;
                height: 2px;
            }

            .main-visual {
                height: 50vh;
                min-height: 350px;
            }

            .shop-info h2 {
                font-size: 1.5rem;
            }

            .note {
                max-width: 100%;
                padding: 0 10px;
            }
        }

        /* 画像が読み込まれない場合の背景 */
        .slide::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            opacity: 0.1;
            z-index: -1;
        }
    </style>
</head>

<body>
    <!-- ヘッダー（画像の上に重なる） -->
    <header>
        <div class="logo">
            <%= link_to root_path do %>
            <%= image_tag 'usagiya.png', alt: '御菓子処うさぎや', class: 'logo-image' %>
            <% end %>
        </div>
        <nav>
            <%= link_to "御菓子処うさぎやの想い", spirit_path %>
            <%= link_to "おすすめ商品", featured_products_path %>
            <%= link_to "季節限定商品", seasonal_products_path %>
            <%= link_to "お知らせ", notices_path %>
            <%= link_to "お問い合わせ", new_inquiry_path %>
            <div class="hamburger" id="hamburger">
                <span></span>
                <span></span>
            </div>
        </nav>
    </header>

    <!-- オーバーレイ -->
    <div class="overlay" id="overlay"></div>

    <!-- サイドメニュー -->
    <div class="side-menu" id="sideMenu">
        <div class="side-menu-close" id="closeMenu">&times;</div>
        <ul class="side-menu-links">
            <li><%= link_to "ホーム", root_path %></li>
            <li><%= link_to "御菓子処うさぎやの想い", spirit_path %></li>
            <li><%= link_to "おすすめ商品", featured_products_path %></li>
            <li><%= link_to "季節限定商品", seasonal_products_path %></li>
            <li><%= link_to "冷凍もちパイの発送", "#delivery" %></li>
            <li><%= link_to "さとふる", "https://www.satofull.jp/products/list.php?s4=18&q=%E3%81%86%E3%81%95%E3%81%8E%E3%82%84&cnt=60&p=1", target: "_blank" %></li>
            <li><%= link_to "お知らせ", notices_path %></li>
            <li><%= link_to "営業カレンダー", "#calendar" %></li>
            <li><%= link_to "LINE", "https://page.line.me/296kbwry?openQrModal=true", target: "_blank" %></li>
            <li><%= link_to "Instagram", "https://www.instagram.com/usagiya.fukui/?hl=ja", target: "_blank" %></li>
            <li><%= link_to "お問い合わせ", new_inquiry_path %></li>
        </ul>
    </div>

    <!-- メインビジュアル（画面いっぱい） -->
    <section class="main-visual">
        <div class="slide active" data-slide-index="0">
            <%= image_tag 'top/mamedaifuku_kusamoti.jpg', alt: '豆大福と草餅' %>
        </div>
        <div class="slide" data-slide-index="1">
            <%= image_tag 'top/usagimanju.jpg', alt: 'うさぎまんじゅう' %>
        </div>
        <div class="slide" data-slide-index="2">
            <%= image_tag 'top/motipai.jpg', alt: 'もちパイ' %>
        </div>
        <div class="slide slide-clickable" data-slide-index="3">
            <%= image_tag 'top/satofuru.jpg', alt: 'さとふる' %>
        </div>
    </section>

    <!-- コンテンツエリア -->
    <div class="content-wrapper">
        <!-- 店舗情報セクション -->
        <section class="shop-info" id="shop-info">
            <h2>店舗情報</h2>
            <div class="info-grid-new">
                <div class="info-column">
                    <div class="info-section">
                        <h3>住所</h3>
                        <p class="postal-code">〒916-0071</p>
                        <p class="address">福井県鯖江市持明寺町5-10-1</p>
                    </div>
                    <div class="info-section">
                        <h3>アクセス</h3>
                        <p>
                            <%= link_to "Googleマップで見る", "https://maps.google.com/?q=福井県鯖江市持明寺町5-10-1", target: "_blank", class: "map-link" %>
                        </p>
                    </div>
                </div>

                <div class="info-column">
                    <div class="info-section">
                        <h3>営業時間</h3>
                        <p class="business-hours">平日・土曜日: 9:00〜18:00</p>
                        <p class="closed-day">定休日: 日曜日</p>
                        <p class="irregular-holiday">(月曜日に不定休あり)</p>
                        <p class="note">※不定休は「お知らせ」または<br>「営業カレンダー」より確認してください</p>
                    </div>
                </div>

                <div class="info-column">
                    <div class="info-section">
                        <h3>電話番号</h3>
                        <p class="phone">
                            TEL: <a href="tel:0778-62-0861">0778-62-0861</a>
                        </p>
                    </div>
                    <div class="info-section">
                        <h3>SNS</h3>
                        <div class="sns-links">
                            <%= link_to 'https://page.line.me/296kbwry?openQrModal=true', target: '_blank', class: 'sns-icon-img' do %>
                            <%= image_tag 'shop/line.png', alt: 'LINE', class: 'sns-image' %>
                            <% end %>
                            <%= link_to 'https://www.instagram.com/usagiya.fukui/?hl=ja', target: '_blank', class: 'sns-icon-img' do %>
                            <%= image_tag 'shop/instagram.png', alt: 'Instagram', class: 'sns-image' %>
                            <% end %>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </div>

    <!-- JavaScriptでスライドショーとハンバーガーメニューを制御 -->
    <script>
        let slideTimer = null;

        function initializePage() {
            console.log('=== 初期化開始 ===');

            if (slideTimer) {
                clearInterval(slideTimer);
            }

            let currentSlide = 0;
            const slides = document.querySelectorAll('.slide');

            console.log('スライド数:', slides.length);

            function showSlide(index) {
                slides.forEach((slide, i) => {
                    if (i === index) {
                        slide.classList.add('active');
                    } else {
                        slide.classList.remove('active');
                    }
                });
            }

            function nextSlide() {
                currentSlide = (currentSlide + 1) % slides.length;
                showSlide(currentSlide);
            }

            slideTimer = setInterval(nextSlide, 5000);

            // 4枚目のスライドにクリックイベント
            const clickableSlide = document.querySelector('.slide-clickable');
            if (clickableSlide && !clickableSlide.dataset.hasListener) {
                clickableSlide.dataset.hasListener = 'true';
                clickableSlide.style.cursor = 'pointer';

                clickableSlide.addEventListener('click', function(e) {
                    console.log('4枚目クリック - タブを開きます');
                    window.open('https://www.satofull.jp/products/list.php?s4=18&q=%E3%81%86%E3%81%95%E3%81%8E%E3%82%84&cnt=60&p=1', '_blank');
                });
            }

            // ハンバーガーメニュー
            const hamburger = document.getElementById('hamburger');
            const sideMenu = document.getElementById('sideMenu');
            const overlay = document.getElementById('overlay');
            const closeMenu = document.getElementById('closeMenu');

            if (hamburger && !hamburger.dataset.initialized) {
                hamburger.dataset.initialized = 'true';

                hamburger.addEventListener('click', function(e) {
                    e.preventDefault();
                    sideMenu.classList.add('active');
                    overlay.classList.add('active');
                });

                closeMenu.addEventListener('click', function(e) {
                    e.preventDefault();
                    sideMenu.classList.remove('active');
                    overlay.classList.remove('active');
                });

                overlay.addEventListener('click', function(e) {
                    e.preventDefault();
                    sideMenu.classList.remove('active');
                    overlay.classList.remove('active');
                });
            }

            console.log('=== 初期化完了 ===');
        }

        if (document.readyState === 'loading') {
            document.addEventListener('DOMContentLoaded', initializePage);
        } else {
            initializePage();
        }
    </script>
</body>

</html>