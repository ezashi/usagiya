<!-- ヘッダー -->
<header class="fixed top-0 left-0 right-0 z-50 bg-transparent">
    <div class="flex items-center justify-between px-8 py-6">
        <!-- ロゴ -->
        <div class="flex items-center space-x-12">
            <%= link_to root_path, class: "text-white text-2xl font-bold tracking-widest" do %>
            うさぎや
            <% end %>

            <!-- ナビゲーション -->
            <nav class="hidden md:flex items-center space-x-8 text-white text-sm">
                <%= link_to "ホーム", root_path, class: "hover:opacity-70 transition-opacity" %>
                <%= link_to "おすすめ商品", "#recommended", class: "hover:opacity-70 transition-opacity" %>
                <%= link_to "季節限定商品", "#seasonal", class: "hover:opacity-70 transition-opacity" %>
                <%= link_to "お知らせ", notices_path, class: "hover:opacity-70 transition-opacity" %>
                <%= link_to "お問い合わせ", new_inquiry_path, class: "hover:opacity-70 transition-opacity" %>
            </nav>
        </div>

        <!-- 言語切替とハンバーガーメニュー -->
        <div class="flex items-center space-x-6">
            <span class="text-white text-sm">JA</span>

            <!-- ハンバーガーメニュー -->
            <button id="hamburgerBtn" class="hamburger flex flex-col justify-center items-center w-8 h-8 space-y-2">
                <span class="hamburger-line block w-8 h-0.5 bg-white"></span>
                <span class="hamburger-line block w-8 h-0.5 bg-white"></span>
            </button>
        </div>
    </div>
</header>

<!-- フルスクリーンメニュー -->
<div id="fullscreenMenu" class="fullscreen-menu hidden fixed inset-0 bg-white z-40 flex items-center justify-center">
    <button id="closeMenuBtn" class="absolute top-8 right-8 text-4xl text-gray-800 hover:opacity-70">×</button>

    <nav class="text-center">
        <ul class="space-y-6 text-2xl">
            <li><%= link_to "おすすめ商品", "#recommended", class: "hover:opacity-70 transition-opacity" %></li>
            <li><%= link_to "季節限定商品", "#seasonal", class: "hover:opacity-70 transition-opacity" %></li>
            <li><%= link_to "冷凍もちパイの発送", new_order_path, class: "hover:opacity-70 transition-opacity" %></li>
            <li><%= link_to "さとふる", "#", target: "_blank", class: "hover:opacity-70 transition-opacity" %></li>
            <li><%= link_to "お知らせ", notices_path, class: "hover:opacity-70 transition-opacity" %></li>
            <li><%= link_to "営業カレンダー", "#", class: "hover:opacity-70 transition-opacity" %></li>
            <li><%= link_to "LINE", "#", target: "_blank", class: "hover:opacity-70 transition-opacity" %></li>
            <li><%= link_to "Instagram", "#", target: "_blank", class: "hover:opacity-70 transition-opacity" %></li>
            <li><%= link_to "お問い合わせ", new_inquiry_path, class: "hover:opacity-70 transition-opacity" %></li>
        </ul>
    </nav>
</div>

<!-- メインビジュアル（フルスクリーン画像スライダー） -->
<section class="relative w-full h-screen overflow-hidden">
    <!-- スライド1 -->
    <div class="hero-slide absolute inset-0 opacity-100 transition-opacity duration-1000">
        <%= image_tag "top/mamedaifuku_kusamoti.jpg", 
        class: "w-full h-full object-cover", 
        alt: "豆大福と草餅" %>
        <div class="absolute inset-0 bg-black bg-opacity-30"></div>
    </div>

    <!-- スライド2 -->
    <div class="hero-slide absolute inset-0 opacity-0 transition-opacity duration-1000">
        <%= image_tag "top/usagimanju.jpg", 
        class: "w-full h-full object-cover", 
        alt: "うさぎまんじゅう" %>
        <div class="absolute inset-0 bg-black bg-opacity-30"></div>
    </div>

    <!-- スライド3 -->
    <div class="hero-slide absolute inset-0 opacity-0 transition-opacity duration-1000">
        <%= image_tag "top/motipai.jpg", 
        class: "w-full h-full object-cover", 
        alt: "もちパイ" %>
        <div class="absolute inset-0 bg-black bg-opacity-30"></div>
    </div>

    <!-- スクロールダウン表示 -->
    <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 text-white text-sm animate-bounce">
        <div class="flex flex-col items-center">
            <span class="mb-2">SCROLL</span>
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
        </div>
    </div>
</section>

<!-- 店舗情報セクション -->
<section class="py-20 bg-white">
    <div class="max-w-4xl mx-auto px-8 text-center">
        <h2 class="text-3xl font-light mb-12 tracking-wider">店舗情報</h2>

        <div class="space-y-6 text-gray-700">
            <div>
                <h3 class="text-lg font-medium mb-2">住所</h3>
                <p>〒XXX-XXXX 東京都XX区XXXX</p>
            </div>

            <div>
                <h3 class="text-lg font-medium mb-2">営業時間</h3>
                <p>平日: 9:00 - 18:00</p>
                <p>土日祝: 9:00 - 17:00</p>
                <p class="text-sm text-gray-500 mt-2">定休日: 水曜日</p>
            </div>

            <div class="flex justify-center space-x-8 mt-8">
                <%= link_to "#", target: "_blank", class: "hover:opacity-70 transition-opacity" do %>
                <div class="flex flex-col items-center">
                    <div class="w-12 h-12 bg-green-500 rounded-full flex items-center justify-center text-white mb-2">
                        <span class="text-xl font-bold">LINE</span>
                    </div>
                    <span class="text-sm">LINE</span>
                </div>
                <% end %>

                <%= link_to "#", target: "_blank", class: "hover:opacity-70 transition-opacity" do %>
                <div class="flex flex-col items-center">
                    <div class="w-12 h-12 bg-gradient-to-br from-purple-500 to-pink-500 rounded-full flex items-center justify-center text-white mb-2">
                        <span class="text-xl font-bold">IG</span>
                    </div>
                    <span class="text-sm">Instagram</span>
                </div>
                <% end %>
            </div>
        </div>
    </div>
</section>

<!-- JavaScript -->
<script>
    // スライドショー
    const slides = document.querySelectorAll('.hero-slide');
    let currentSlide = 0;

    function showSlide(index) {
        slides.forEach((slide, i) => {
            slide.style.opacity = i === index ? '1' : '0';
        });
    }

    function nextSlide() {
        currentSlide = (currentSlide + 1) % slides.length;
        showSlide(currentSlide);
    }

    // 4秒ごとにスライド切り替え
    setInterval(nextSlide, 4000);

    // ハンバーガーメニュー
    const hamburgerBtn = document.getElementById('hamburgerBtn');
    const fullscreenMenu = document.getElementById('fullscreenMenu');
    const closeMenuBtn = document.getElementById('closeMenuBtn');

    hamburgerBtn.addEventListener('click', () => {
        fullscreenMenu.classList.remove('hidden');
        fullscreenMenu.classList.add('visible');
        document.body.style.overflow = 'hidden';
    });

    closeMenuBtn.addEventListener('click', () => {
        fullscreenMenu.classList.remove('visible');
        fullscreenMenu.classList.add('hidden');
        document.body.style.overflow = 'auto';
    });

    // メニューリンククリックで閉じる
    fullscreenMenu.querySelectorAll('a').forEach(link => {
        link.addEventListener('click', () => {
            fullscreenMenu.classList.remove('visible');
            fullscreenMenu.classList.add('hidden');
            document.body.style.overflow = 'auto';
        });
    });
</script>