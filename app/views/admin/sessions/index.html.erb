<!DOCTYPE html>
<html>

<head>
    <title>管理者ログイン - 御菓子処うさぎや</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
</head>

<body style="margin: 0; padding: 0;">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        }

        .login-wrapper {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #1e3a8a 0%, #3b82f6 100%);
            padding: 20px;
        }

        .login-container {
            max-width: 450px;
            width: 100%;
        }

        .login-box {
            background-color: white;
            border-radius: 16px;
            padding: 40px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        }

        .login-header {
            text-align: center;
            margin-bottom: 32px;
        }

        .company-name {
            font-size: 24px;
            font-weight: 700;
            color: #1e3a8a;
            margin: 0 0 8px 0;
        }

        .login-title {
            font-size: 16px;
            font-weight: 500;
            color: #64748b;
            margin: 0;
        }

        .error-box {
            background-color: #fef2f2;
            border: 1px solid #fecaca;
            color: #991b1b;
            padding: 12px 16px;
            border-radius: 8px;
            margin-bottom: 24px;
            font-size: 14px;
        }

        .error-content {
            display: flex;
            align-items: flex-start;
        }

        .error-icon {
            width: 20px;
            height: 20px;
            margin-right: 8px;
            flex-shrink: 0;
            margin-top: 2px;
        }

        .error-list {
            margin: 0;
            padding-left: 0;
            list-style: none;
        }

        .error-list li {
            margin-bottom: 4px;
        }

        .error-list li:last-child {
            margin-bottom: 0;
        }

        .login-form {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .form-label {
            display: block;
            color: #334155;
            font-weight: 600;
            font-size: 14px;
        }

        .form-label .required {
            color: #dc2626;
            margin-left: 4px;
        }

        .form-input {
            appearance: none;
            border: 1px solid #cbd5e1;
            border-radius: 8px;
            width: 100%;
            padding: 12px 16px;
            color: #1e293b;
            font-size: 15px;
            line-height: 1.5;
            transition: all 0.2s;
            box-sizing: border-box;
        }

        .form-input:focus {
            outline: none;
            border-color: #3b82f6;
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
        }

        .form-input::placeholder {
            color: #94a3b8;
        }

        .form-hint {
            font-size: 12px;
            color: #64748b;
            margin: 0;
        }

        .submit-button {
            width: 100%;
            background-color: #3b82f6;
            color: white;
            font-weight: 600;
            font-size: 15px;
            padding: 12px 24px;
            border-radius: 8px;
            cursor: pointer;
            border: none;
            transition: all 0.2s;
            margin-top: 8px;
        }

        .submit-button:hover {
            background-color: #2563eb;
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(59, 130, 246, 0.4);
        }

        .submit-button:active {
            transform: translateY(0);
        }

        @media (max-width: 640px) {
            .login-box {
                padding: 32px 24px;
            }

            .company-name {
                font-size: 20px;
            }
        }
    </style>

    <div class="login-wrapper">
        <div class="login-container">
            <div class="login-box">
                <div class="login-header">
                    <h1 class="company-name">御菓子処うさぎや</h1>
                    <p class="login-title">管理者ログイン</p>
                </div>

                <% if flash[:alert].present? %>
                <div class="error-box">
                    <div class="error-content">
                        <svg class="error-icon" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
                        </svg>
                        <div>
                            <% if flash[:alert].is_a?(Array) %>
                            <ul class="error-list">
                                <% flash[:alert].each do |error| %>
                                <li><%= error %></li>
                                <% end %>
                            </ul>
                            <% else %>
                            <%= flash[:alert] %>
                            <% end %>
                        </div>
                    </div>
                </div>
                <% end %>

                <%= form_with url: admin_login_path, method: :post, html: { novalidate: true, class: "login-form" } do |f| %>
                <div class="form-group">
                    <%= label_tag :login_id, class: "form-label" do %>
                    管理者ユーザーID<span class="required">*</span>
                    <% end %>
                    <%= text_field_tag :login_id, params[:login_id], 
                        class: "form-input", 
                        autofocus: true,
                        placeholder: "管理者ユーザーIDを入力" %>
                </div>

                <div class="form-group">
                    <%= label_tag :password, class: "form-label" do %>
                    パスワード<span class="required">*</span>
                    <% end %>
                    <%= password_field_tag :password, nil, 
                        class: "form-input",
                        placeholder: "パスワードを入力（7文字以上）" %>
                    <p class="form-hint">※パスワードは7文字以上で入力してください</p>
                </div>

                <div>
                    <%= submit_tag "ログイン", class: "submit-button" %>
                </div>
                <% end %>
            </div>
        </div>
    </div>

</body>

</html>