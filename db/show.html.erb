<% content_for :page_title, "商品詳細" %>

<style>
    .product-detail-container {
        padding: 2rem;
        max-width: 1400px;
        margin: 0 auto;
    }

    .detail-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 2rem;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .header-left {
        flex: 1;
        min-width: 0;
    }

    .back-link {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        color: var(--admin-text-secondary);
        text-decoration: none;
        margin-bottom: 1rem;
        transition: color 0.2s ease;
    }

    .back-link:hover {
        color: var(--admin-accent);
    }

    .product-title {
        color: var(--admin-text);
        font-size: 2rem;
        font-weight: 700;
        margin: 0;
        word-wrap: break-word;
    }

    .header-actions {
        display: flex;
        gap: 0.75rem;
        flex-wrap: wrap;
    }

    .product-layout {
        display: grid;
        grid-template-columns: 400px 1fr;
        gap: 2rem;
    }

    .product-image-section {
        position: sticky;
        top: 2rem;
        height: fit-content;
    }

    .product-image-card {
        aspect-ratio: 1;
        border-radius: 12px;
        overflow: hidden;
        background-color: var(--admin-bg-secondary);
        border: 1px solid var(--admin-border);
    }

    .product-image-card img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .no-image-placeholder {
        width: 100%;
        height: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        color: var(--admin-text-muted);
    }

    .placeholder-icon {
        width: 5rem;
        height: 5rem;
        margin-bottom: 1rem;
    }

    .product-info-section {
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
    }

    .status-badge {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.5rem 1rem;
        border-radius: 9999px;
        font-size: 0.875rem;
        font-weight: 600;
    }

    .status-badge.published {
        background-color: rgba(16, 185, 129, 0.1);
        color: var(--admin-success);
    }

    .status-badge.draft {
        background-color: rgba(107, 114, 128, 0.1);
        color: var(--admin-text-muted);
    }

    .status-dot {
        width: 0.5rem;
        height: 0.5rem;
        border-radius: 50%;
        background-color: currentColor;
    }

    .info-grid {
        display: grid;
        gap: 1rem;
    }

    .info-item {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    .info-label {
        color: var(--admin-text-secondary);
        font-size: 0.875rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.05em;
    }

    .info-value {
        color: var(--admin-text);
        font-size: 1.125rem;
    }

    .price-value {
        font-size: 2rem;
        font-weight: 700;
        color: var(--admin-primary);
    }

    .description-content {
        color: var(--admin-text);
        line-height: 1.75;
    }

    .description-content img {
        max-width: 100%;
        height: auto;
        border-radius: 8px;
        margin: 1rem 0;
    }

    .category-tags {
        display: flex;
        gap: 0.5rem;
        flex-wrap: wrap;
    }

    .category-tag {
        padding: 0.375rem 0.75rem;
        background-color: var(--admin-bg-secondary);
        border: 1px solid var(--admin-border);
        border-radius: 6px;
        font-size: 0.875rem;
        color: var(--admin-text-secondary);
        display: inline-flex;
        align-items: center;
        gap: 0.375rem;
    }

    .category-tag.active {
        background-color: rgba(59, 130, 246, 0.1);
        border-color: var(--admin-accent);
        color: var(--admin-accent);
    }

    .tag-icon {
        width: 1rem;
        height: 1rem;
    }

    .delete-warning {
        background-color: rgba(239, 68, 68, 0.05);
        border: 1px solid rgba(239, 68, 68, 0.2);
        border-radius: 8px;
        padding: 1rem;
        margin-top: 2rem;
    }

    .warning-text {
        color: var(--admin-error);
        font-size: 0.875rem;
        margin: 0;
    }

    @media (max-width: 1024px) {
        .product-layout {
            grid-template-columns: 1fr;
        }

        .product-image-section {
            position: static;
        }
    }

    @media (max-width: 768px) {
        .product-detail-container {
            padding: 1rem;
        }

        .detail-header {
            flex-direction: column;
            align-items: flex-start;
        }

        .header-actions {
            width: 100%;
        }

        .header-actions .admin-btn {
            flex: 1;
            justify-content: center;
        }

        .product-title {
            font-size: 1.5rem;
        }
    }
</style>

<div class="product-detail-container">
    <!-- ヘッダー -->
    <div class="detail-header">
        <div class="header-left">
            <%= link_to admin_products_path, class: "back-link" do %>
            <svg style="width: 1.25rem; height: 1.25rem;" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
            </svg>
            商品一覧に戻る
            <% end %>
            <h1 class="product-title"><%= @product.name %></h1>
        </div>

        <div class="header-actions">
            <%= link_to edit_admin_product_path(@product), class: "admin-btn admin-btn-secondary" do %>
            <svg style="width: 1.25rem; height: 1.25rem;" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
            </svg>
            編集
            <% end %>

            <%= link_to admin_product_path(@product), 
                  method: :delete, 
                  data: { confirm: "「#{@product.name}」を削除してもよろしいですか？\n\n※この操作は取り消せません。商品一覧からも完全に削除されます。" },
                  class: "admin-btn admin-btn-danger" do %>
            <svg style="width: 1.25rem; height: 1.25rem;" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
            </svg>
            削除
            <% end %>
        </div>
    </div>

    <!-- 商品詳細 -->
    <div class="product-layout">
        <!-- 左カラム: 画像 -->
        <div class="product-image-section">
            <div class="product-image-card">
                <% if @product.image.attached? %>
                <%= image_tag @product.image, alt: @product.name %>
                <% else %>
                <div class="no-image-placeholder">
                    <svg class="placeholder-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                    </svg>
                    <p>画像なし</p>
                </div>
                <% end %>
            </div>
        </div>

        <!-- 右カラム: 情報 -->
        <div class="product-info-section">
            <!-- 公開ステータス -->
            <div class="admin-card">
                <div class="admin-card-body">
                    <% if @product.visible? %>
                    <span class="status-badge published">
                        <span class="status-dot"></span>
                        公開中
                    </span>
                    <% else %>
                    <span class="status-badge draft">
                        <span class="status-dot"></span>
                        下書き
                    </span>
                    <% end %>

                    <% if @product.published_at %>
                    <p style="color: var(--admin-text-muted); font-size: 0.875rem; margin: 0.75rem 0 0 0;">
                        公開日時: <%= @product.published_at.strftime("%Y年%m月%d日 %H:%M") %>
                    </p>
                    <% end %>
                </div>
            </div>

            <!-- 基本情報 -->
            <div class="admin-card">
                <div class="admin-card-header">
                    <h2 class="admin-card-title">基本情報</h2>
                </div>
                <div class="admin-card-body">
                    <div class="info-grid">
                        <div class="info-item">
                            <div class="info-label">価格</div>
                            <div class="price-value">¥<%= number_with_delimiter(@product.price) %></div>
                        </div>

                        <div class="info-item">
                            <div class="info-label">商品ID</div>
                            <div class="info-value">#<%= @product.id %></div>
                        </div>

                        <div class="info-item">
                            <div class="info-label">作成日</div>
                            <div class="info-value"><%= @product.created_at.strftime("%Y年%m月%d日 %H:%M") %></div>
                        </div>

                        <div class="info-item">
                            <div class="info-label">最終更新</div>
                            <div class="info-value"><%= @product.updated_at.strftime("%Y年%m月%d日 %H:%M") %></div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- カテゴリー -->
            <div class="admin-card">
                <div class="admin-card-header">
                    <h2 class="admin-card-title">カテゴリー</h2>
                </div>
                <div class="admin-card-body">
                    <div class="category-tags">
                        <span class="category-tag <%= 'active' if @product.featured? %>">
                            <svg class="tag-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z"></path>
                            </svg>
                            おすすめ商品
                        </span>

                        <span class="category-tag <%= 'active' if @product.seasonal? %>">
                            <svg class="tag-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"></path>
                            </svg>
                            季節限定商品
                        </span>
                    </div>
                </div>
            </div>

            <!-- 商品説明 -->
            <div class="admin-card">
                <div class="admin-card-header">
                    <h2 class="admin-card-title">商品説明</h2>
                </div>
                <div class="admin-card-body">
                    <div class="description-content">
                        <%= @product.description %>
                    </div>
                </div>
            </div>

            <!-- 削除に関する警告 -->
            <div class="delete-warning">
                <p class="warning-text">
                    <strong>⚠️ 注意:</strong> 削除ボタンを押すと、この商品は商品一覧から完全に削除されます。おすすめ商品や季節限定商品に設定されている場合もすべて削除されます。この操作は取り消せません。
                </p>
            </div>
        </div>
    </div>
</div>